language: java
sudo: false
script: mvn clean verify
before_install:
  - mvn install:install-file -Dfile=libs/FloatingPassage.jar \
     -DgroupId=com.blademc.uselesswaifu -DartifactId=FloatingPassage \
     -Dversion=1.0 -Dpackaging=jar
